import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        borderRadius: 30,
        borderWidth: 2,
       

    },

    image: {
        borderRadius: 30,

    }
});

export default styles;